// Calculate the area of circle
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int r;
    cin >> r;

    double area = M_PI * r * r;
    cout << fixed << setprecision(2) << area << endl;
    return 0;
}
